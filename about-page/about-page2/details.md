### 1. Fonts:
NAvbar: logo is an image, the menu font is Inter, 
Header: the title font is "Playfair Display" 36px
Main section: the title  font is "Playfair Display" 48px
Paragraphs: font is Inter 24px,

### 2. Colors:
background is white, text in the main section is black, footer is #f9f9f9, footer text is black and #7f7f7f

### 3. Spacing system:
The whole page width on large screen is 1440px but the content does 1110px wide, gap between 2 flex-row elements is 36px, gap between sections is around 154px, gap inside section between elements on vertical axis (title, image, paragraph) is around 48px, gap between elements groups on grid is horizontaally 27px annd vertically aroung 36px.